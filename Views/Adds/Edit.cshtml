@model Page.Models.Add

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Add</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AddId" />
            <div class="form-group">
                <label>Mã số</label>
                <input asp-for="AddId" class="form-control" />
                <span asp-validation-for="AddId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Tên</label>
                <input asp-for="AddName" class="form-control" />
                <span asp-validation-for="AddName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
